import 'package:flutter/material.dart';

class AppBorders {
  static const BorderRadius cardRadius = BorderRadius.all(Radius.circular(24));
  static const BorderRadius buttonRadius = BorderRadius.all(Radius.circular(18));
  static const BorderRadius inputRadius = BorderRadius.all(Radius.circular(16));
  static const BorderSide border = BorderSide(color: Color(0xFFF8BBD0), width: 1.5);
  static const BorderRadius dialogRadius = BorderRadius.all(Radius.circular(20));
}
