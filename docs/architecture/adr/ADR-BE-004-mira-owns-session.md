# ADR-BE-004: Mira owns Beauty Session

## Decision

Sessions, attempts, looks, history, favorites, collections, and share records are Mira-owned. Vendors never own session state.

## Consequences

Adapters are replaceable; session store is the system of record.
