# State → Visual Mapping

All states from 9B FaceCaptureReadinessState.
Painter reacts to state (dim for multipleFaces, scale cue for distance, pose tick, hold ring, ready accent).
No independent UI readiness logic.
