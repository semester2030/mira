# FK-1 — Knowledge Registry Architecture

Capabilities: lookup, versioning, activate/deactivate, supersession, source review, domain/occasion/confidence/cultural filters, trend expiry, conflict links, audit history.

**Initial storage decision:** versioned JSON under a future `mira-api` knowledge package (git-reviewed) → later hybrid DB for scale.

Not `assets/fashion/knowledge_graph.json` (SKU graph).
