# Provenance Matrix

All `CANONICAL_FACE` assignments in projector attach to statements derived from stored report or server region policy constant.  
`focus.publicFactAr` / `focus.reasonAr` never assigned to `statementAr`.  
Independent adversarial: provenance injection strings not present in sealed envelope.
