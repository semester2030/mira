# Runtime Report — Phase 6C

Garment Intelligence **emits** existing Fashion Runtime states only (`AVAILABLE` · `PARTIAL` · `DEGRADED` · `BLOCKED` · …).

- Schema / enum vocabulary: **unchanged** (6B freeze)
- `providerId` stripped on public DTOs
- Stage used: `mapping`
- Capability id on runtime: `analyze_garment`
