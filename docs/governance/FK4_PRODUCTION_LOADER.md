# FK-4 — Production Loader

- Validate on load
- Fail closed on invalid registry
- No silent mock fallback
- Reject allowTestOnly / testOnly in production path
- Explicit UNAVAILABLE / INVALID states
- Optional cached parsed registry after validation
