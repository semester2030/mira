# FK-4 — Registry Release Model

## FashionKnowledgeRegistryRelease
releaseId, registryVersion, snapshotId, activeRuleCount, domainsCovered, releasedAt, releaseNotes, approvedBy?, rollbackTarget?

FK-4 ships empty production registry — no production release with real rules.
