# PHASE 9L — Rollback Audit

Flags OFF → legacy capture + legacy beauty report path restored.  
Verified by ResultsReportEntry gate and CapturePanel flag branches.

Residual: FaceExperienceTokens gradient on NewAnalysisScreen even when flags OFF (presentation-only, not feature activation).
