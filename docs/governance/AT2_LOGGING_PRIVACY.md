# AT-2 — Logging / Privacy

Safe: model, latencyMs, traceId, HTTP status class, token counts (via cost sink)
Not logged by default: API key, full user payload, full provider JSON, CoT, raw canonical context
