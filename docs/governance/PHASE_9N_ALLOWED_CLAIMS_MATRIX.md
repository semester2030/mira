# Allowed Claims Matrix

`allowedClaims` built from sealed unit claimKeys only (`envelope-builder`).  
Client text cannot add claim keys. Adversarial sealed envelopes exclude attractiveness score and forged prose.
