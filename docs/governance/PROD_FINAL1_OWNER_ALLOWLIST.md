# PROD-FINAL-1 — Owner Allowlist

**Status:** NOT CONFIGURED in production

Configure `MIRA_PRODUCTION_INTERNAL_UIDS` in Render secrets with the owner Firebase UID only.
Governance may store a redacted reference (last 4 chars) after set — never full UID in git.
