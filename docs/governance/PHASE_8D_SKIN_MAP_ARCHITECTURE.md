# PHASE 8D — Skin Map Architecture

```
Hub skinMap tab
  └─ ResultsSkinMapPanel
       ├─ title/badge/confidence (+ stale badge)
       ├─ info sheet (required Mode B explanation)
       ├─ concern ChoiceChips (eligible only)
       ├─ AnimatedBeautyReportFaceMap (illustrative overlay)
       ├─ concern detail (status, explanation, one action, limitation)
       └─ Ask Mira
```

Mode locked: `MapPresentationMode.illustrativeUserImage` / Mode B.
