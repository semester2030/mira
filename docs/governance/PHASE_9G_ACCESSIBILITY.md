# PHASE 9G — Accessibility
Region semantics: «منطقة X — افتحي التفاصيل». Sheet order: title → result → explanation → limitation → action.
`FaceDetailAccessibilityList` when accessibleNavigation. Reduce Motion supported. RTL sheets.
