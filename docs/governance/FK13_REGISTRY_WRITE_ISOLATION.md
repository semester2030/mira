# FK13 REGISTRY WRITE ISOLATION

Public barrel excludes storage/release. Telemetry `assertNoRegistryWriteSurface`. Feedback mapping forces `activatesRule: false`, `writesRegistry: false`. No Advisor→activate path found. Deep-import write surfaces remain (convention boundary).
