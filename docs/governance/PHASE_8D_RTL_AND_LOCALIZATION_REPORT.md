# PHASE 8D — RTL and Localization Report

- Hub and sheets forced RTL in test harness (`TextDirection.rtl`, `Locale('ar')`)
- Public copy Arabic only; no provider/version/MCE English leaks (test coverage)
- Number direction via Flutter RTL; status text not raw English enums
- Large text scale golden + widget coverage
