# FK-11 — Security Audit

- No public FK HTTP controllers found
- Projection forbids provider/raw draft fields by construction
- Prompt injection resisted on *library* narration path (tests)
- Public barrel exports test fixtures with ACTIVE rules + mock provider (**MAJOR soft boundary**)
- `saveDraftRegistry` exported via registry barrel (**MAJOR soft boundary** if misused)
