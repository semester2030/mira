# Canonical Face Evidence Policy

`canonical_face_report` / `face_intelligence` provenance ONLY when:
- Stored authoritative report exists
- Statement derived from stored fields
- Selection ref resolves (when focused)
- Freshness/eligibility flags honored

No self-asserted provenance from client text.
