# AT-4R — Model Conformance

## Documented / template model
`gpt-4o-mini` (`LLM_MODEL` in `.env.qa.example`)

## Runtime assessment
**NOT RUN** — no live call.

When live:
- STRUCTURED_OUTPUT_PASS / FAIL
- ARABIC_PASS / FAIL
- POLICY_PASS / FAIL

AT-4R does not change model unless separately approved and current config cannot function.
