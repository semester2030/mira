# PHASE 8E — Today Action Integration

`immediateAction.routineStepId` == `personalPlan.todayStepId`.
Selection deterministic from priorities ↔ step concepts, else SPF, else first step.
No filler when plan ineligible — today action null → empty card.
