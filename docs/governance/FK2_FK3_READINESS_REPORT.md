# FK-2 — FK-3 Readiness Report

FK-3 may proceed when it:
1. Produces only `FashionAdviceCandidateDraft`
2. Maps to LLM_GENERAL_KNOWLEDGE + UNCURATED provenance
3. Runs `evaluateFashionClaimLock` before any user-facing narration
4. Does not call Advisor / public outfit analyze APIs directly with unlocked text
5. Does not populate curated registry (that is FK-4/FK-5)

**Ready for FK-3.**
