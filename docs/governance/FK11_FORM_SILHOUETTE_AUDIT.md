# FK-11 — Form / Silhouette Audit (FK-7)

Law #37 encoded in `engineering-law-37.ts` + tone-safety body patterns.  
Mock scenarios `form_body_slimming` / `form_body_shape` exist for negative tests.  
Production surfaces: when MCE flag off, body-related fashion answers may still be possible via unrestricted MCE (MAJOR under default config).
