# Guidance Reconciliation

Requires resolvable `frozenRecommendationRef` (or guidance id → reco).  
Unresolved → `face_context_guidance_ref_unresolved` and **no** recommendation units.  
Reason unit uses **stored** `rec.reasonAr` only.
