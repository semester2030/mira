# FK-1 — Alternative Generation

Advice directions are goal-ranked, not right/wrong.

Example directions (architecture only):
- Preserve bold identity → neutral accessories
- Reduce contrast → replace one dominant color with neutral
- Raise formality → material/accessory formality cues

Each alternative is its own `FashionAdviceCandidate` linked by `alternativeGroupId`.
