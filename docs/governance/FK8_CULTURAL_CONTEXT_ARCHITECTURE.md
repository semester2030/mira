# FK-8 — Cultural Context Architecture

Explicit context → normalize → registry Mode A (empty) → optional Mode B → Claim Lock.

OI modesty: CONSUME_ONLY. Locale ≠ identity. Culture is context, not judgment.
