# PROD-FINAL-1 — Server Enforcement

`AdvisorService.chat`: Fashion Mode B / integration path requires `fashionAdvisorModeB` entitlement for authenticated Firebase UID. Non-owner → `integrationEnabled: false` (fail closed Option A behavior).

Face Intelligence evidence projection remains ownership-scoped via Prisma `userId` (existing 9M).
