# AT-2 — Cost / Usage Hook

`tokenUsage.promptTokens/completionTokens` returned on provider result.
FK-3 orchestrator feeds `FashionLlmCostSink` on success/fail/retry.
Operational usage ≠ user analytics telemetry (still OFF).
