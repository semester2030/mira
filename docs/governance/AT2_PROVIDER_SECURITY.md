# AT-2 — Provider Security

- API key server-only via ConfigService / env
- HTTPS base URL enforced
- No key in logs
- No full prompt/response logged by default
- Provider cannot set Mira knowledge authority (mapper forces UNCURATED)
