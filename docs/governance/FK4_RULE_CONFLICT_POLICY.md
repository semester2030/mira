# FK-4 — Rule Conflict Policy

Registry **reports** conflicts via relations (CONFLICTS, SPECIALIZES, EXCEPTION_TO, SUPPORTS).
Registry does **not** perform user-specific final conflict resolution.
Future runtime / Claim Lock decides advice eligibility.
