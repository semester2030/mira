# FK13 CONTEXT TRUST AUDIT

Assembler sets `existingKnowledgeRuleRefs: []`. No approvalStatus/sourceType/ruleId fields on public DTO. Cultural only if `culturalContextExplicit===true`. User evidenceRefs are refs only — cannot escalate provenance authority.
