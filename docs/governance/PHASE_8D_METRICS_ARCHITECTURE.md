# PHASE 8D — Metrics Architecture

```
Executive Summary
  └─ entry «المؤشرات»
       └─ ResultsMetricsMapHubScreen (metrics tab)
            ├─ MetricsOverviewSection (visible metrics only)
            └─ MetricDetailSheet (progressive disclosure)
                 └─ Ask Mira (route + grounded question)
```

Ownership: Law #35 — each metric has one meaning, one evidence source, one confidence state, one recommended action, one presentation owner (`MetricPresentationPolicy` + metric VM).
