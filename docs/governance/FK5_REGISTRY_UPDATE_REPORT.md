# FK-5 — Registry Update Report

Production `registry.json` **not** populated with ACTIVE rules.

Review candidates live in `curated/*.ts` + inventory mirror JSON under `assets/.../review-candidates/`.

Promotion requires Tier A/B + proven human approval (`filterReleaseEligibleCandidates`).
