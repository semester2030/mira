# AT-2 — Advisor Service Integration

`AdvisorService` already injects optional port; AT-2 registers production provider.
Integration test enables flags only inside test process env, injects provider with mocked fetch.
Flutter still unused for this path (AT-3).
