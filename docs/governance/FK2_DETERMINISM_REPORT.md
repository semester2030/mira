# FK-2 — Determinism Report

Same candidate + context + claim-lock version → identical decision, gate outcomes, reason/qualification codes, confidenceCap, claim strength.

Clock is explicit (`context.clock.nowIso`); no wall-clock inside Claim Lock.

Evidence: `ok determinism` in `test:fk2`.
