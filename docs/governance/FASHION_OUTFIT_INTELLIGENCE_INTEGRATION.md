# FK-1 — Outfit Intelligence Integration

Consume-only:
```
CanonicalGarment[] (+ optional CanonicalOutfit evidence projection)
→ FKL
```

No OI mutation. Prefer no new OI fields.  
If a future OI field is needed → Change Request classification; FK prefers working from garments + occasion + existing evidence ids.

HTTP analyze may remain garments-only; FKL orchestration can run after client/server obtains occasion + garments without claiming OI ran.
