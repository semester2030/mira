# AT-2 — Prompt Policy Compliance

Provider consumes frozen `FASHION_LLM_SYSTEM_PROMPT` via `buildFashionLlmPrompt`.
No parallel fashion prompt system.
No chain-of-thought requested or stored.
Injection patterns filtered in prompt builder (FK-3).
