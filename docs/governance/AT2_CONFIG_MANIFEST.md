# AT-2 — Config Manifest (for later AT-4)

Already in Render: `LLM_API_KEY` (sync:false), `LLM_BASE_URL`, `LLM_MODEL`, `LLM_TEMPERATURE`
Required later to enable Mode B (NOT applied now):
- `FASHION_KNOWLEDGE_ADVISOR_INTEGRATION_ENABLED=true`
- `FASHION_KNOWLEDGE_LLM_ENABLED=true`
Keep false: telemetry, legacy MCE fashion, registry/domains unless tested
