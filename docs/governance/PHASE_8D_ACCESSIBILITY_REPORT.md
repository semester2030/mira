# PHASE 8D — Accessibility Report

- Status communicated via text labels + icons/chips, not color alone
- Metric cards expose Semantics
- Map ExcludeSemantics with parent Semantics text equivalent
- Touch targets ≥ ~46–48 height on CTAs/chips
- Detail sheets dismissible (modal + drag handle)
- Selected concern announced via chip selected state + Semantics label
- Map limitation available via visible copy + info sheet
