# PHASE 9G — Detail Sheet Architecture
Mirror-first + contextual bottom sheets (D11).
Assembler (pure) → Router → `showFaceDetailSheet` → `FaceDetailSheet` sections.
Hierarchy: WHAT → OBSERVATION → MEANING → CONFIDENCE/LIMITATION → ONE CTA → Ask Mira.
